#SPDX-License-Identifier: MIT-0
---
# tasks file for test
- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Copy nginx configuration file
  copy:
    src: ./test/files/index.html
    dest: /usr/share/nginx/html/
  notify: Restart nginx

- name: Debug app status
  debug:
    msg: "App {{ app_name }} is running on port {{ port }}"

